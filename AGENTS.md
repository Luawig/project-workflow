# AGENTS

This repository is the source of the `project-workflow` skill, not a consuming application.

- Agent entry is root `SKILL.md`.
- When changing the workflow, update `SKILL.md` and the matching `references/` files. Do not restore removed parallel workflow files.
- Templates and examples must not include personal paths, private project names, or internal service URLs.
